# criando objetos e funções no R

vetor <- c(1, 2, 3, 5, 7) # criação de um vetor

str(vetor) # estrutura do vetor

regCarros <- lm(mpg ~ ., mtcars) # criação de um modelo de regressão

str(regCarros) # estrutura da regressão
